// UI Component library exports
import Button from './Button';
import Icon from './Icon';
import Modal from './Modal';

// Export components individually and as a collection
export {
  Button,
  Icon,
  Modal,
};

// Default export as a UI object for convenience
const UI = {
  Button,
  Icon,
  Modal,
};

export default UI;